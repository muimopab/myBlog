export default {
    bind(el) {
        window.onload = function () {
            let DOM = document.body.getElementsByClassName("markdown-body")[0]
            let a = document.getElementById("_1")
            let arr = new Array;
            arr = a
            let titles = []
            // for (let i = 0; i < a.length; i++) {
            //     console.log(i)
            //     console.log(a[i])

            // }

        }
    }
}